import { ParentLoginMain } from '@/app/parent/login/ParentLogin_components/ParentLoginMain';

export const metadata = {
  title: 'Parent Login - Smart PG',
};

export default function ParentLogin() {
  return <ParentLoginMain />;
}
