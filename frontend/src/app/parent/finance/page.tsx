import { ParentFinanceMain } from '@/app/parent/finance/ParentFinance_components/ParentFinanceMain';

export const metadata = {
  title: 'Finance - Parent Portal',
};

export default function ParentFinance() {
  return <ParentFinanceMain />;
}
