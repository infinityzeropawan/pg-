import { StudentSettingsMain } from '@/app/student/settings/StudentSettings_components/StudentSettingsMain';

export const metadata = {
  title: 'Settings | Student Portal',
  description: 'Manage your preferences and settings',
};

export default function StudentSettingsPage() {
  return <StudentSettingsMain />;
}
