import { StudentFeedbackMain } from '@/app/student/feedback/StudentFeedback_components/StudentFeedbackMain';

export const metadata = {
  title: 'Feedback & Ratings | Student Portal',
  description: 'Provide feedback and ratings for PG services',
};

export default function StudentFeedbackPage() {
  return <StudentFeedbackMain />;
}
